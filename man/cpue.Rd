% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cpue.R
\name{cpue}
\alias{cpue}
\title{cpue}
\usage{
cpue(data, effectifs, debut, fin)
}
\arguments{
\item{data}{A data.frame containing the data.}

\item{effectifs}{A numeric column representing captured quantities.}

\item{debut}{A column with start dates (must be in \code{Date} or \code{POSIXct} format).}

\item{fin}{A column with end dates (must be in \code{Date} or \code{POSIXct} format).}
}
\value{
A data.frame grouped by \code{debut} and \code{fin}.
\itemize{
\item \code{effectifs}: Sum of captured quantities for each group.
\item \code{nb_jours}: Number of days between start and end dates.
\item \code{cpue}: Catch per unit effort (calculated as \code{effectifs / nb_jours}).
}
}
\description{
cpue
}
\examples{
data <- data.frame(
  debut = as.Date(c("2024-01-01", "2024-01-15", "2024-01-22")),
  fin = as.Date(c("2024-01-03", "2024-01-29", "2024-02-04")),
  effectifs = c(10, 20, 30))
cpue(data,effectifs,debut,fin)
}
